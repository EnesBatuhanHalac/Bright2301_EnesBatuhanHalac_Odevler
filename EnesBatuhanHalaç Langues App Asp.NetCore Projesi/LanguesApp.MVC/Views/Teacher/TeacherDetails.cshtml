@model TeacherViewModel

<div class=" text-center mt-3">

    <div class="text-primary ">
        <h1>@Model.Name @Model.LastName Detay Bilgisi</h1>
    </div>

    <div >
        <img class="cart-img-top" style="width:300px" src="~/images/@Model.ImageUrl" alt="@Model.Name">

        <h1 class="mt-3 "> <i>@Model.Name @Model.LastName </i> </h1>
        <div class="mt-3">
        <p> <span class="text-danger fw-bold">PriceForHours: </span>@Model.Price.ToString("c")</p>
        <p> <span class="text-danger fw-bold">Country:</span>  @Model.Country</p>
        <p> <span class="text-danger fw-bold">About:</span> @Model.About</p>
        <p>  <span class="text-danger fw-bold">Teacher'sAge: </span> @Model.Age</p>
        <p>  <span class="text-danger fw-bold">Email:</span>  @Model.Email</p>
        <p>  <span class="text-danger fw-bold">Teacher Phone Number:</span>  @Model.PhoneNumber</p>
        </div>
    </div>

</div>